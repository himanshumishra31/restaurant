<% if model.errors.any? %>
  <div class="error-explanation">
    <h2 class="errors-heading"><%= pluralize(@user.errors.count, "error") %> prohibted this user from being saved </h2>
    <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
